[package]
name = "injm"
description = "A lightweight CLI tool for code injection"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
clap = { version = "4.6.1", features = ["derive", "cargo"] }
tree-sitter-language-pack = "1.12.0"
